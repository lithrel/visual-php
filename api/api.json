[
    {
        "name": "add",
        "type": "int",
        "params": [
            {"name": "$a", "type": "int", "default": 0},
            {"name": "$b", "type": "int", "default": 0}
        ],
        "source": "function (int $a, int $b) { return $a + $b; }"
    },
    {
        "name": "multiply",
        "type": "int",
        "params": [
            {"name": "$a", "type": "int", "default": 0},
            {"name": "$b", "type": "int", "default": 0}
        ],
        "source": "function (int $a, int $b) { return $a * $b; }"
    }
]